# 编译脚本说明

本目录包含后端和前端的编译脚本。

## 📜 脚本列表

### build-all.sh - 全量编译

**功能**: 编译后端和前端所有模块

**用法**:
```bash
bash build-all.sh
```

**执行步骤**:
1. 编译后端: `mvn clean package -DskipTests -T 1C`
2. 编译前端: `npm install && npm run build`

### build-backend.sh - 后端编译

**功能**: 仅编译后端模块，并生成所有JAR文件到 `lib/` 目录

**用法**:
```bash
bash build-backend.sh
```

### build-frontend.sh - 前端编译

**功能**: 编译前端，生成静态文件

**用法**:
```bash
bash build-frontend.sh
```
